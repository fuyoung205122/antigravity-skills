根據您提供的原始 Skill 內容，以下是為 **Geopolitical Strategy Analyst Watcher** 自動產生的使用說明書：

---

# Geopolitical Strategy Analyst Watcher

# 功能說明

自動監控特定資料夾中的網址或文字檔，並調用專業分析助手產出地緣政治戰略評估報告。

# 解決什麼問題

在資訊爆炸的時代，手動收集並分析地緣政治新聞非常耗時。此 Skill 解決了「手動提取資訊」與「重複性分析流程」的痛點，讓使用者只需將感興趣的連結丟入資料夾，即可自動獲得深度分析，實現情報收集自動化。

# 適合什麼情境

1. **每日新聞追蹤**：將感興趣的政治新聞連結存入資料夾，自動產出分析筆記。
2. **研究資料彙整**：蒐集多篇關於特定區域衝突的文章，統一由 AI 進行戰略評估。
3. **影音內容轉文字分析**：將 YouTube 政治評論影片連結存入，快速掌握核心論點與地緣影響。
4. **情報監控工作流**：建立自動化情報站，減少開啟瀏覽器手動複製貼上的動作。
5. **長期趨勢追蹤**：定期產出的 Markdown 報告便於搜尋與存檔，建立個人地緣政治資料庫。

# 如何觸發

此 Skill 為「自動監控型」，一旦啟動後，您只需在指定路徑執行以下操作即可觸發：

* **範例 1**：在資料夾中新增一個 `news.txt`，內容寫入一個新聞網址。
* **範例 2**：直接拖入一個包含網址的 `.url` 捷徑檔案。
* **範例 3**：建立一個 `.md` 檔案，裡面貼上一則關於國際局勢的報導內容與連結。
* **範例 4**：批次放入多個文字檔，系統會每分鐘檢查並逐一處理。
* **範例 5**：透過自動化腳本將網路爬蟲抓到的連結儲存至該目錄。

# 輸入內容

* **檔案格式**：`.txt`, `.url`, `.md` 檔案。
* **必要資訊**：檔案內需包含至少一個有效的網際網路網址（URL）。
* **存放路徑**：`E:\2026andigravity2\10-youtube\youtube政治`。

# 輸出內容

1. **地緣政治報告**：在同一資料夾產生檔名結尾為 `_地緣政治分析.md` 的 Markdown 報告。
2. **檔案歸檔**：處理過的原始檔案會自動移至 `processed` 子資料夾，保持工作區整潔。

# 注意事項

1. **路徑依賴**：必須確保 `E:\2026andigravity2\10-youtube\youtube政治` 路徑存在，否則 Skill 可能無法正確執行。
2. **配置檔案**：此 Skill 需讀取同目錄下的 `analyst_prompt.md` 作為分析邏輯，請確保該檔案內容完整。
3. **檢查頻率**：系統預設每 1 分鐘檢查一次資料夾。
4. **排除機制**：系統會自動跳過已分析過的檔案（`_地緣政治分析.md`）以避免無限循環。

# 常見問題

* **Q：為什麼放入檔案後沒有反應？**
  * A：請確認 Skill 是否已成功啟動並顯示「Watcher active」。另外檢查檔案是否包含可辨識的 URL。
* **Q：分析報告的深度可以調整嗎？**
  * A：可以，請修改 `./analyst_prompt.md` 中的系統提示詞（System Prompt）來調整分析的觀點或細節。
* **Q：它可以分析 YouTube 影片嗎？**
  * A：是的，只要該網址能被解析，系統會嘗試讀取內容並進行地緣政治分析。

---